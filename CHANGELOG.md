# 更新日志

本文件记录 dsh-desktop 各版本的主要特性变化。历史版本中的常规修复不再赘述，仅保留安全相关的加固记录。

## [0.2.0] - 2026-08-16

### 新增
- **内置[安全市场](https://github.com/bruc3van/dsh-desktop-safe-market)** [0.2.4](https://github.com/bruc3van/dsh-desktop-safe-market/releases/tag/v0.2.4)：随安装包发布，启动时自动接入正在运行的 profile，设置里出现「插件市场」导航项——「插件」页是精选目录与已安装面板（查看 / 停用 / 卸载通过包装的插件），目录里已经装过的插件标出「已安装 vX.Y.Z」并改走「安全升级」；「技能」页列出当前会话实际可用的技能；没有工作区时一次点击即可完成初始化。市场**默认关闭**，开启后才会联网读取目录，关闭时这一页只剩开关本身
- 接入方式沿用官方的 in-box bundle 机制：插件随客户端运行时闭包发布，客户端只往 profile 的 `dsh.profile.bundles` 里加一项、并建一条指向闭包的软链——**不写依赖、不改锁文件、不跑 pnpm、不联网**。升级插件即升级客户端
- 只在使用内置运行时时接入：插件的 `@deepseek-ai/*` 依赖从客户端闭包向上解析，交给你自己安装的 dsh 会让那个运行时拿到第二份 Service 类。智能模式解析运行时、复用已有实例、已安装 dsh、固定地址这四种情况下一律撤回该项
- 接入失败可自愈：内置运行时未就绪而客户端座位仍在时自动撤回，并在本会话不再重试——插件在加载期抛错会导致**整棵插件树**启动失败，共用同一 profile 的 CLI 也会跟着起不来。全新 `DSH_HOME` 首次启动时 profile 尚不存在，就绪后再写入，供下一次启动加载
- 绝不写坏你的 profile：插件目录缺失、或 `profiles/node_modules` 上已有非软链目录时，不写入 `bundles` 并撤回已有条目，避免官方 `loadProfile` 因解不出 listed bundle 而让共用 `~/.dsh` 的 CLI 一起起不来
- 你自己装过该插件（作为 profile 依赖）且版本不低于内置时，客户端完全不插手；比内置更旧的覆盖则改用闭包那份（收回依赖、清掉 profile 里更近的旧安装）
- 客户端升级后，指向旧闭包的接入软链会自动改指新路径，无需重新接入

### 改进
- npx 缓存的 dsh 版本低于内置运行时时给出提示：智能模式仍优先使用你自己的缓存（那是你上一次 `npx @deepseek-ai/dsh` 留下的），但连接设置里会说明内置的更新，重新跑一次 npx 即可刷新缓存

### 修复
- 修复同一 `DSH_HOME` 下可能并发运行两个 harness 导致会话日志永久损坏（`invalid persisted inbox splice` / `corrupt session log`）：客户端现在把自己启动的运行时记录在 `DSH_HOME` 下，下次启动优先接管仍在服务的遗留进程（复用同一 harness，会话照常共享），接管不了才清理后重启；万一遗留进程既连不上也结束不掉，客户端会拒绝启动并说明原因，而不是在它旁边再写一份。感谢 @Wildareasilence 的问题定位与修复思路（#3、#4）
- 更新安装不再可能把运行时留给安装器：Windows 上停止运行时的时机从「拉起安装器之后 400ms」改到「安装包下载并通过 SHA-256 校验之后、启动安装器之前」。此前安装器按进程名杀掉应用时匹配不到 `node.exe` 运行时子进程，旧运行时成为孤儿继续写 `~/.dsh`，新版本启动后又起一个，两个写入者并发损坏会话日志。下载或校验失败、以及安装器最终没能启动时，运行时都会保住或自动恢复（#3）
- 智能模式现在会读取 web profile patch 层（`~/.dsh/profiles/web/cordis.patch.yml`）里配置的端口一并探测：官方 `dsh web` 的端口除 `--port` 外也可写在该文件里，此前客户端只探测默认的 3080，用户把实例挪到其他端口后会探测不到、进而在旁边另起一个 harness（#3）
- 修复 Windows 深色模式下窗口外框持续闪烁：外观模式的引导猜测（页面颜色 vs 系统主题）此前每次上报都重算，主进程固定 `themeSource` 后渲染进程的 `matchMedia` 随之翻转，模式在 fixed/system 之间来回拉扯成循环。现在引导猜测每个页面只执行一次并写入记忆，显式点击外观选项或打开设置面板时仍会覆盖它（#2）

### 安全
- 加固更新安装包的文件名校验：除路径分隔符外，`:`（Windows 盘符相对路径 `C:evil`、NTFS 备用数据流 `setup.exe:x`）、末尾的点与空格（Windows 会静默丢弃，导致写入与校验落在两个名字上）一并中和，纯 `.`／`..` 直接拒绝，`CON`、`PRN` 等设备名加前缀规避。此前只替换了分隔符，上述形式仍可能把下载写到 `updates` 目录之外

## [0.1.10] - 2026-08-15

### 修复
- Windows 左键点击托盘图标可直接打开主窗口：此前只有 macOS 注册了点击事件，Windows 上只能通过右键菜单里的「显示主窗口」恢复窗口
- 在线更新改走 Chromium 网络栈（Electron `net.fetch`），沿用系统代理、PAC 与操作系统证书库；Node 自带的 fetch 两者都不认，Windows 上因此出现「能查到新版本、下载却报 fetch fail」的情况（安装包与更新清单不在同一主机上），失败时回退到 Node fetch
- 更新失败的原因不再只显示一句 `fetch failed`：展开 `cause` 链（DNS、TLS、连接被重置、代理拒绝等）后一并显示

### 改进
- 「应用更新」区域新增链接图标，点击直接打开 GitHub 发布页手动下载（官方设置面板中的增强卡片与客户端连接设置页两处一致）

## [0.1.9] - 2026-08-15

### 改进
- 原生界面跟随 Web UI 的语言设置：菜单栏、托盘与对话框读取客户端的语言偏好（`$DSH_HOME/settings.yaml` 中的 `locale.preference`），在页面里切换语言后菜单实时重建；尚未设置或连接远程页面时仍按系统语言显示
- macOS 菜单栏改为完整中英文文案：此前「关于…／隐藏…／服务」等条目沿用 Electron 内置英文标签，与自定义的「检查更新…」混排；现补齐编辑、显示、窗口菜单的本地化标题与标准快捷键
- 安装包瘦身：Chromium 语言包只保留中文与英文（约减少 44 MB／45 MB），发布构建只保留目标平台的 node-pty 预编译二进制，并额外裁剪 `.pdb`、`.ts` 等运行时无法执行的构建产物；README 与英文文档同步更新桌面端体验说明

### 构建与发布
- 新增客户端身份指纹校验（`pnpm run check:identity`）：发布流程在构建前核对 `package.json` 与 `electron-builder.yml` 的 `name`、`main`、`appId`、`productName`。此前 manifest 曾被误写为 Electron 默认 app，整条流水线仍会构建成功并发出安装包，只是产品已被替换
- 新增 TypeScript 入口点守卫：若未来有依赖以 `.ts` 作为运行入口，会在发布任务中带包名报错，而不是让裁剪后的安装包在启动时才失败
- 打包冒烟测试断言语言包裁剪结果（Windows 与 macOS 各自的命名），语言包被静默删除会直接让构建变红

## [0.1.8] - 2026-08-15

### 修复
- 「下载并安装」不再像没有反应：点击后立即显示状态，下载过程显示进度条与百分比／已下载大小；被拒绝的安装（缺少 SHA-256、无可安装版本、桥接调用被拒）会把原因写在卡片上，不再静默返回
- Windows 启动安装程序改用可处理 UAC 提权的方式，并在启动失败时报告原因（此前 `spawn` 失败的错误无人接收，点击后没有任何提示）
- macOS 下载完成后提示「退出并替换」：镜像打开时应用仍在运行，Finder 会拒绝替换「应用程序」中的旧版本（提示“正在使用中”），更新因此无法完成

### 改进
- 更新日志按 Markdown 渲染（标题、列表、代码、引用、表格、链接），不再显示原始标记；应用内设置卡片、客户端连接设置页与系统更新提示框三处一致（提示框渲染为纯文本）
- 更新日志里的链接可从连接设置页点开（改为交给系统浏览器），下载过程中日志区不再被重绘、滚动位置与选中内容得以保留
- 安装被拒绝（如缺少 SHA-256）时保留“发现新版本”状态：托盘入口与「下载并安装」按钮仍在，可直接重试；远程页面看不到本机原因时给出对应说明，取消确认不再显示为失败

## [0.1.7] - 2026-08-15

### 改进
- 放大 Windows 任务栏、窗口与安装包图标中的白色鲸鱼字形，在小尺寸下与其他应用保持更一致的视觉重量，同时不改变原有深色圆角底板和 macOS 图标
- 首次启动提示改为明确的 10–20 秒预期，并在等待 10 秒、20 秒和 35 秒后动态更新进度说明，减少长时间准备运行时造成的卡死误解

### 修复
- Windows 托盘固定使用白色字形，修复“Windows 模式为深色、应用模式为浅色”时错误显示黑色图标、在深色任务栏上难以辨认的问题

## [0.1.6]

### 新增
- 应用内更新：读取 GitHub Releases 检查、下载并安装新版本
- 连接设置面板改版：更新入口与其他设置项对齐，桌面端/服务版本号移至更新卡片展示
- 智能模式与固定地址模式可随时切换，切回智能模式时保留已保存地址，之后可一键重新固定
- 智能模式复用的运行时实例失联时自动改用客户端启动的运行时；固定地址失败时提供“切换到智能模式”出口
- 窗口外观与系统原生样式保持一致，新增页面右键菜单（编辑、链接、图片）
- 连接失败改为应用内错误界面，直接给出地址、原因与恢复入口，不再弹出原生对话框（窗口已关闭时仍回退为原生提示）

### 改进
- 内置运行时不再把 `ELECTRON_RUN_AS_NODE` 泄漏给 Agent：该变量只在运行时自身重新拉起 Node 子进程（原生目录选择器、Windows ACL 沙箱执行器）时于 spawn 边界补回，Agent 执行的命令不再继承它，因此运行 `code` 等基于 Electron 的工具不会再以纯 Node 模式启动而失败
- 发布版把 Electron 内置的 Node 以 `node` 之名发布到 `~/.dsh-desktop/bin` 并追加到运行时 PATH 末尾：未安装 Node.js 的用户，Agent 也能执行 `node`；已安装的用户仍优先使用自己的版本
- macOS 读取登录 shell PATH 时优先使用交互式登录 shell，找回写在 `~/.zshrc` 里的工具目录（如 `~/.local/bin`），失败时退回原有的非交互登录 shell
- macOS 安装包补充文件访问用途说明，系统在询问桌面、文稿、下载等目录权限时会说明 Agent 的用途
- Windows 托盘图标随系统明暗主题自动切换，避免在浅色或深色任务栏上失去对比度
- GitHub Release 自动读取当前版本 CHANGELOG，清晰列出版本变化、安装包选择和 macOS／Windows 安装步骤

### 安全
- 打包环境下更新源地址与 GitHub API 地址禁止被环境变量覆盖，避免更新流程被劫持到非官方地址
- 打包环境下数据目录（`DSH_HOME`、`DSH_DESKTOP_HOME`）及连接探测开关禁止被环境变量覆盖：被植入的数据目录等同于被植入的连接配置，可静默改写客户端每次启动连接的服务器地址
- 校验更新清单中的安装包文件名，防止路径穿越写出到下载目录之外
- 收紧渲染进程权限请求：仅放行剪贴板写入与全屏，摄像头、麦克风等设备权限一律拒绝
- 拦截页面内的越权导航与新窗口跳转，仅允许跳转到可信来源

## [0.1.5] - 2026-08-14

### 新增
- Win32 目录选择器兼容性检测

## [0.1.0] - 2026-08-13

首个发布版本。

### 新增
- 系统托盘支持
- DeepSeek 连接凭据配置与设置卡片，附 API Key 创建入口提示
- 应用内 API 契约（Zod Schema），覆盖 agent 预设、审批、命令、事件、目标、主机、LLM、RPC、会话、技能、任务、工作区等模块
- 本地运行时启动失败时的友好错误提示
- 加载页新增加载图标与状态提示
