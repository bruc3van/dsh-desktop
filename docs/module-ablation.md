# 桌面模块消融记录

2026-09-07，基线 `b6d2b57`。本轮处理更新状态、主入口转发、设置保存和插件恢复四个候选。

| 模块 | 删除或收敛 | 保留的职责 |
| --- | --- | --- |
| 更新状态 | 删除 `checking` / `isChecking`，删除检查结束时的重复通知 | `phase` 驱动界面；`checkInFlight` 合并并发检查；下载和安装防重复 |
| 主入口 | 删除 11 个纯转发函数，直接调用或传递控制器方法 | 13 个提前注入的回调仍延迟访问控制器，避免初始化前读取 |
| 设置保存 | IPC 与本地入口共享来源校验、远程确认和保存逻辑；删除公开的绕过校验保存入口及内部转发层 | IPC 发送方验证、远程确认、串行保存、占用检查、独立环境来源约束 |
| 插件恢复 | 依赖和 bundles 的重复解析收敛为一个内部读取函数 | 卸载前识别直接插件；卸载后重新读取并验证；读取失败时不判定成功 |

验证结果：

- `check:updater`：真实 Electron 设置卡片、检查、下载、SHA-256、失败重试和超时通过。新增检查覆盖同一次网络请求的并发检查、失败后的重试，以及每次检查只有开始和结果两次状态通知。
- `check:safety`：新增真实 IPC handler 到设置命令的测试，覆盖未授权发送方、无效输入、取消确认、同意确认和本地保存。既有数据模式竞态、进程保护和安装器失败恢复通过。
- `check:plugin-isolation`：真实 Electron 批量卸载、无关插件保留、独立环境切换、installed/npx 来源故障和客户端插件撤座重试通过。
- `check:settings-server`、`check:connection-controller`、构建、类型检查、lint 和 diff 格式检查通过。

没有删除生命周期保护，也没有引入通用控制器框架或缓存插件 manifest。本轮属于代码和状态结构简化，不声称改善启动或安装耗时。
